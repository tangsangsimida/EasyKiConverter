# Golden Files

This directory stores expected outputs for golden-file tests.

- `kicad/`: Expected `.kicad_sym`, `.kicad_mod`, 3D model references, `fp-lib-table` snippets, and export reports.

Golden outputs should be normalized before comparison when paths, line endings, timestamps, or platform-specific separators are involved.
